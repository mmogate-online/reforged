# Session Resume: IoD Restoration (paused 2026-07-18)

Read this FIRST when continuing. Self-contained handoff for a fresh session.

## The pending strategic decision (decide FIRST, before any more work)

The user has decided to **redo the Island of Dawn baseline restoration using the
old client v17.11 (`D:\dev\tera\tera-dc-17_11\DataCenter_Final_USA`) as the
north star, filling gaps from v31 only when v17.11 lacks the data.** HOW to
execute this is explicitly NOT yet decided and must be worked out in the new
session, not assumed.

**Why the pivot:** the current v92 IoD baseline came from a v31-sourced
migration. This session then layered old-client (v17.11) archaeology on top
(re-enabling disabled quests, reconstructing mob spawns from client fences).
Mixing the two sources without a precedence rule produced observable
DUPLICATION in-game: duplicate/overabundant quests, duplicated mob spawns, an
inflated quest count. v31 and classic-era (v17.11) are not the same content
set; v92 (from v31) carries quests/spawns the classic island did not have, and
vice versa. The fix is a single source of truth: v17.11 primary, v31 gap-fill.

**Sequencing constraint (binding):** the restored classic baseline must be
committed (baseline lane) BEFORE the patch-001 DSL specs are applied on top.
Right now they are intermingled uncommitted (see below), which is the mess to
untangle.

## Current deployed / committed state

- Dev game server: latest deploy is everything below (server datasheet SSH
  mirror, 101 dirty files). World-server restart is manual; user was mid-test.
- Client launcher dev channel: published through **0.1.0-dev.11** (each dev.N
  this session added the IoD content deltas; records in
  `reforged-deploy\patcher-origin\reforged.rporigin` + `deployment-log.md`).
- Local git (datasheet repo `D:\dev\mmogate\tera92\server`, files under
  `Datasheet/`): **HEAD = 9c7163fe (clean IoD v31 baseline). NOTHING from this
  session is committed.** 101 files dirty in the working tree, mixing BOTH
  lanes:
  - BASELINE lane (restoration, this session): `QuestData/*.quest` (33+ quests
    re-enabled + spine relinks + 1311 auto-accept->NPC-accept + 059901
    Stepstone disable), `QuestGroupList.xml` (story-group registrations),
    `CompensationData/QuestCompensationData_13.xml` (75 v31 reward rows),
    `TerritoryData_13.xml` + `TerritoryData_213.xml` (17 mob groups + 8
    villagers + Leander's Outpost + Eria relocation).
  - TUNING lane (patch 001 DSL, pre-existing uncommitted): `CCompensation_*`,
    `ECompensation_*`, `Equipment*`, `Item*`, `Passivity*`, `MaterialEnchant`,
    `Gacha`, `NpcData_13/436` (balance), etc.
- The content repo `reforged/` also has uncommitted working changes (tools,
  docs, plan updates); user commits the DSL/patch lane, Claude may commit the
  baseline lane once validated (see [[project-commit-lanes]]).

## What the redo likely has to reconsider (for the new session to decide)

- Whether to re-run the IoD migration (`docs/migrations/island-of-dawn/`) with
  inverted source precedence (v17.11 primary), or to surgically reconcile the
  current tree against v17.11.
- How to treat quests/spawns present in v31 but absent in classic v17.11
  (candidates for REMOVAL/disable, the opposite of this session's re-enabling).
- The `dc-restore` tools already diff all three sources; `audit_quests` and
  `survey` can be repointed to make v17.11 the authority instead of v31.
- Whether this session's restoration work is kept, partially kept, or discarded
  and redone. Do not assume; the user will decide.

## Validated in-game this session (may or may not survive the redo)

Kishale yellow quests (1322/1323, class rewards), 1334/1341 repeatables (after
collection fixes 404/405->410/411 and 403->409), Batch 3 restored mob habitats,
Leander's Outpost relocation to the authentic spot (55422,-82484). Full spine
re-enabled through Redeployment.

## Known open issues

1. **1311 "Redeployment" auto-accept trap.** Converting it to NPC-accept did
   NOT unblock the user's already-stuck character (they still used
   `/@start_quest 1311`). Root cause: the char completed prereq 1310 while 1311
   was disabled; the missed auto-grant is a character-DB state, not fixable by
   editing the quest for an in-progress char. Re-verify on a FRESH char after
   the redo. The NPC-accept conversion should help fresh chars; confirm.
2. **deploy-client publish blocked by running game client.** The patcher build
   fails ("Scanning" / cannot access `TERA_d3d9.log`) whenever the game client
   is open (it locks that log). Happened 3x. HARDENING TODO: make
   `tools/deploy-client/deploy_client.py` detect a running client and either
   skip the publish stage with a clear message or auto-retry when the lock
   clears. Not yet done.
3. **AUTOGRANT_CHAIN audit check** proposed but not built: flag `즉시수주`
   quests sitting behind a prerequisite (fragile under staged re-enable).
4. Stepstone `To Help by Gathering` (59901) was disabled via 99,99 sentinel
   this session; keep or revisit under the redo.

## Tools built this session (all under `reforged/tools/`)

- `dc-restore/`: `dclib.py` (shared: refs loader, 3-source resolution, ns-strip,
  client shard indexer, git-HEAD baseline reader, XML comment scanner),
  `survey.py` (3-source gap report), `dcq.py` (cross-source query:
  quest/npc/name/collection), `audit_quests.py` (12-flag deterministic quest
  auditor), `quest_restore.py` (sentinel re-enable, prereq relink, story-group
  registration), `comp_restore.py` (v31 comp merge), `spawn_restore.py`
  (territory/mob/villager reconstruction from client fences). README.md covers
  all. Registered in `reforged/CLAUDE.md` workflow table.
- `deploy-client/`: `deploy_client.py` (one-command sync -> pack -> install ->
  CF publish with version bump + rporigin/log recording). README.md present.
  Registered in CLAUDE.md.

Reference data in `docs/plans/iod-alpha-content-loop/data/`:
`legacy-quest-locations.md` (wiki quest->camp map), `2026-07-17-ruins-archaeology.md`,
plus the audit outputs `iteration-0-gap-report.md/.json`,
`iteration-2-quest-audit.md/.json`, `batch-3-spawn-plan.md/.json`.

## First actions for the new session

1. Re-read this file and `PLAN.md` (the batch model is now superseded by the
   redo decision).
2. Present options for the v17.11-baseline redo (re-migrate vs reconcile) and
   let the user choose. Do not start editing datasheets until that is decided.
3. Preserve the uncommitted baseline-lane work (or snapshot it) before any
   destructive reconcile, so it is not lost if the redo reuses parts of it.
