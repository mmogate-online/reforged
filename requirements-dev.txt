# Developer tooling for the Python toolkits under reforged/tools/.
# Install with: python -m pip install -r reforged/requirements-dev.txt
# Runtime dependencies (openpyxl) are listed in CLAUDE.md prerequisites.
pytest>=8.0
