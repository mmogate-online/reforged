# Island of Dawn region strings, aligned to the classic set.
#
# Every classic region name in this zone already matches, so the only op left is
# the removal of 13035 "Ruined Temple", a later name whose section does not
# exist in the classic zone.
#
# provenance: ported-from-v31

spec:
  version: "1.0"
  schema: v92

regionStrings:
  delete:
    - 13035
