[pytest]
testpaths = tools/dc-restore/tests
pythonpath = tools/dc-restore
addopts = -q --strict-markers
markers =
    corpus: needs the private v92 server datasheet repo, read at the pinned baseline commit
filterwarnings = error
