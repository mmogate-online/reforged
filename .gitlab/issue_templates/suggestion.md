<!--
For a change request or a design idea, not a defect. If something is broken, use bug.md.
-->

### The suggestion

<!-- One or two sentences, stated as an outcome rather than an implementation. -->

### What problem it solves

<!-- Who is affected and how often. A suggestion with no problem behind it is a preference, and
     preferences are decided by the design framework, not by issue volume. -->

### What it would change

| | |
|---|---|
| System | <!-- quest, loot, npc, enchant, gear, spawn, client, launcher, web --> |
| Scope | <!-- one zone, one class, server-wide --> |

### Anything it conflicts with

<!-- Progression pacing, currency separation, seasonal wipes and cosmetic-only monetization are
     locked design invariants. A suggestion that crosses one is not automatically rejected, but the
     conflict has to be named here rather than discovered later. -->

/label ~"type::suggestion" ~"status::triage"
